# Optional development default — users can configure or override this in Settings at runtime.
# Expo exposes vars prefixed EXPO_PUBLIC_ to client code.
EXPO_PUBLIC_POCKETBASE_URL=https://booksapi.somedomain.com/
